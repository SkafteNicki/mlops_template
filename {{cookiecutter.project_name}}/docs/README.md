Generating the docs
----------

Use [mkdocs](http://www.mkdocs.org/) structure to update the documentation. 

Build locally with:

```bash
mkdocs build
```

Serve locally with:

```bash
mkdocs serve
```